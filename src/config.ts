/**
 * Site-wide configuration.
 *
 * GA_MEASUREMENT_ID: the Google Analytics 4 measurement ID for wireedmpro.com.au.
 * Find it in GA under Admin > Data streams > Web (it looks like "G-XXXXXXXXXX").
 * Leave empty to disable analytics entirely; the tag also never loads in dev builds.
 */
export const GA_MEASUREMENT_ID = "G-LWG4BVB87J";
